function mostrar()
{
	var numeroIngresado;
	var numero;

	numeroIngresado=prompt("Ingrese un numero");
	numeroIngresado=parseInt(numeroIngresado);
	

	for(numero=0;numeroIngresado<numero==0;numero++)
	{
		divisor=numeroIngresado%numero;
		
		if(divisor==0)
		{
			console.info("Divisores: ", numero);
		}
	}
}//FIN DE LA FUNCIÓN